namespace Serious_Game_Na_sciezce_zycia;

public enum Menu { None, Main, LevelSelect, Hints, Options, Dialogue, VerifyAnswer, WinLevel }
public enum Texture {pixel, single, bump, sprite_forward, sprite_back, sprite_side }
public enum QuestionState { none, selectAnswer, correctAnswer, incorrectAnswer, initializeDialogue}
