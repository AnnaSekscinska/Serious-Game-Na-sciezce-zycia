namespace Serious_Game_Na_sciezce_zycia;

public enum Menu { None, Main, LevelSelect, Hints, Instruction, Dialogue, VerifyAnswer, WinLevel, LoseScreen, NameSelect, HelloName, WinGame, LevelBegin }
public enum Texture {pixel, single, bump, sprite_forward, sprite_back, sprite_side, heart, dialogue, sad_face, happy_face, facts, instruction, main_menu, level_select, dude_alive, dude_dead }
public enum QuestionState { none, selectAnswer, correctAnswer, incorrectAnswer, initializeDialogue}
